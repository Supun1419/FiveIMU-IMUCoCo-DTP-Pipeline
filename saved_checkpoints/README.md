# Checkpoints

Run `python tools/download_checkpoints.py` to place the official public files
here. Checkpoint binaries are excluded from Git because the DTP checkpoint is
larger than GitHub's 100 MB per-file limit.
