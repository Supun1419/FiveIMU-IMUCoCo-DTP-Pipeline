# MQTT Parser

`mqtt_firmware_subscriber.py` contains the payload parser used by the runtime.
The expected topic names are `Center_Batch` and `Node1_Batch` through
`Node4_Batch` below `FYP_IMU_Session/`.
